package academy.learnprogramming.util;

// plural class will hold all mappings not just a single -
// the class will only contain public static constants so the class never
// need to be extended or instantiated throughout the application ()cutoff access where it is not needed)
public final class Mappings {

    // constants
    public static final String ITEMS = "items";
    public static final String ADD_ITEM = "addItem";
    public static final String DELETE_ITEM = "deleteItem";

    // constructor
    private Mappings() {

    }
}
